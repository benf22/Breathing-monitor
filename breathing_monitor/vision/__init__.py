"""MediaPipe FaceLandmarker wrapper + model management.

This is the one place that imports mediapipe. Everything downstream consumes
the plain dataclasses from :mod:`breathing_monitor.core.types`.
"""
