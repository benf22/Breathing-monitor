# Breathing Monitor — Phase 1 (detection core) dependencies.
# Pinned to reasonably recent, CPU-only wheels. Everything here installs
# without a GPU and runs on a plain Windows/Linux/macOS machine.

mediapipe==0.10.21        # FaceLandmarker task (face detect + 478 landmarks), CPU
opencv-python==4.11.0.86  # camera capture + image encode/decode
numpy>=1.26,<2.3          # array math (pinned range compatible with mediapipe)
fastapi==0.115.6          # local web dashboard + settings API
uvicorn[standard]==0.34.0 # ASGI server for FastAPI
requests==2.32.3          # one-time model download on first run

# Dev / test only:
pytest==8.3.4
