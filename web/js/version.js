// Single source of truth for the app version shown in the UI and logged to the
// console. Bump this on each meaningful change so you can tell which build a
// device is running. Keep the service-worker cache name (sw.js) in step.
export const APP_VERSION = "0.23.0";
export const APP_BUILD = "2026-07-19";
